stages:
  - test

test_job:
  stage: test
  image: python:3.12

  before_script:
    - python -m pip install --upgrade pip
    - pip install pytest
    # Si tienes requirements.txt:
    # - pip install -r requirements.txt

  script:
    - pytest

  only:
    - master
    - merge_requests